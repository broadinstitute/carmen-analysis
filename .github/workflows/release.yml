name: release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist + wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true

# PyPI publishing intentionally deferred. When ready:
#   1. Configure trusted publishing on pypi.org for this project +
#      workflow=release.yml + environment=pypi.
#   2. Add a `pypi-publish` job here that downloads dist/ and uses
#      pypa/gh-action-pypi-publish@release/v1, plus `id-token: write`
#      in permissions and an `environment: pypi` block on the job.
